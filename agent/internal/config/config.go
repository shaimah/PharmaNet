package config

import "fmt"

// LoadConfig is a placeholder for reading YAML/JSON
func LoadConfig(path string) {
    fmt.Println("Loading config from:", path)
    // TODO: parse YAML file with allowed_apps, token, cache_path
}
